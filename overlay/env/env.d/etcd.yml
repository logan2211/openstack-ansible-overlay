---
component_skel:
  etcd:
    belongs_to:
      - etcd_all
container_skel:
  etcd_container:
    belongs_to:
      - infra_containers
      - shared-infra_containers
    contains:
      - etcd
    properties:
      service_name: etcd
